# T-001 — Fleet status landing page

- **status:** promoted
- **builder_id:** frontend-claude-subagent
- **verifier_id:** verify-claude
- **evidence_sha:** 03f2d2299976eeb269f0452ff111fbbb1e9ca5e4

## AC

1. A single self-contained file `src/index.html` — zero external network
   resources (no CDN scripts, fonts, images, or stylesheets).
2. Content: a header with the project name "Agent Fleet", a hero section
   with a one-line value statement and a status line reading
   "v0 test run · T-001", three feature cards (Dispatch, Critique,
   Evidence — each with a 1–2 sentence description true to this repo's
   AGENTS.md), and a footer naming the git repo path.
3. Core content fully visible with JavaScript disabled.
4. Semantic HTML: one `h1`, landmarks (`header`/`main`/`footer`),
   `lang` set; any decorative graphics carry empty `alt` or are CSS.
5. Legible and unbroken at 375px and 1280px viewport widths (no
   horizontal scroll at 375px).
6. Visible keyboard focus on any interactive element; if no interactive
   elements are needed, that is acceptable.
7. Light and dark scheme support via `prefers-color-scheme`.

## Frontend

(builder appends here)

- 2026-08-06 frontend-claude-subagent: Built `src/index.html` as a single
  self-contained file — inline CSS only, no scripts, no external resources.
  Structure: `header` (project name), `main` with hero (`h1` value
  statement + "v0 test run · T-001" status line) and three feature cards
  (Dispatch, Critique, Evidence) as a semantic `ul`, `footer` with repo
  path. Card copy is drawn from AGENTS.md working agreements. Light/dark
  via `prefers-color-scheme` custom properties + `color-scheme`. Cards use
  `auto-fit/minmax` grid so they stack at 375px; `overflow-wrap: anywhere`
  on the footer path guards against horizontal scroll. No interactive
  elements (AC 6 permits this); a global `:focus-visible` outline exists
  anyway. Ambiguities flagged: (a) AC says "the git repo path" — I used the
  absolute local path `/Users/digitalalchemist/Development/sagan`
  since no remote is specified in the context pack; (b) the one-line value
  statement is my paraphrase of AGENTS.md's purpose, no canonical tagline
  exists. Not verified in a browser — layout/AC 5 claims are by reading
  only.

## QA

(verify appends here)

- 2026-08-06 verify-claude — **overall PASS**, evidence bound to SHA
  `03f2d2299976eeb269f0452ff111fbbb1e9ca5e4`. Rendered headless Chromium
  (Playwright, `file://` URL, no JS injection); screenshots at
  `.sagan/ledger/T-001/{375,1280}-{light,dark}.png`, inspected by the
  verifier.
  - AC1 PASS — external-URL grep (`src/href/url()/@import`, http/https/
    protocol-relative) over `src/index.html`: zero matches; inline CSS only.
  - AC2 PASS — screenshots show header "Agent Fleet", hero h1 ("A minimal
    test bed for contract-driven agent orchestration."), status pill
    "v0 test run · T-001", three cards, footer repo path. Card attestation
    vs AGENTS.md:
    - Dispatch card ↔ "No work is dispatched before the ticket's Acceptance
      Criteria block is written" + "Every dispatch, verdict, and evidence
      record is appended to `.sagan/ledger/events.jsonl`" — TRUE.
    - Critique card ↔ "Builders never self-approve. The critic never
      fixes." + "Critic verdicts: `APPROVED | REVISE | NEEDS_EVIDENCE |
      ESCALATE`" + "Critic without AC is opinion; critic against AC is a
      contract" — TRUE.
    - Evidence card ↔ "APPROVED means verified, not plausible —
      user-visible artifacts need execution evidence recorded at a git SHA
      before they count as done" — TRUE (near-verbatim).
  - AC3 PASS — `grep -c '<script'` = 0; static HTML/CSS renders fully.
  - AC4 PASS — exactly one `<h1>`; `<header>/<main>/<footer>` landmarks;
    `html lang="en"`; zero `img`/`svg` elements (decoration is CSS).
  - AC5 PASS — at 375px, `document.documentElement.scrollWidth` = 375 =
    innerWidth (light and dark): no horizontal scroll. All four screenshots
    legible and unclipped; cards stack at 375px, three-across at 1280px.
  - AC6 PASS — zero interactive elements (permitted by AC); global
    `:focus-visible` outline rule present (src/index.html:102).
  - AC7 PASS — `@media (prefers-color-scheme: dark)` + `color-scheme:
    light dark`; dark screenshots show a distinct dark palette.
  - not_verified: (none).

## Decisions

- 2026-08-06 PM: AC written before dispatch per sagan.yaml
  `ac_before_dispatch: required`. Critique mandatory (user-visible).
  Ship requires critic APPROVED + verify evidence at SHA.
